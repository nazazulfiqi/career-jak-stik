import { atom } from "recoil";

export const modalOtp = atom({
    key: 'modal-otp', 
    default: false, 
  });

export const modalOtpSuccess = atom({
    key: 'modal-otp-success', 
    default: false, 
  });
 
